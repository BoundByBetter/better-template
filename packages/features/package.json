{
  "name": "@boundbybetter/features",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "author": "Stewart Armbrecht",
  "license": "MIT",
  "scripts": {
    "check-deps": "npx npm-check-updates && npx expo install --check",
    "update-deps": "export YARN_ENABLE_IMMUTABLE_INSTALLS=false && npx npm-check-updates -u && yarn install && npx expo install --fix",
    "lint": "eslint .",
    "build": "tsc -b",
    "test": "jest --color",
    "cover": "jest --color --coverage"
  },
  "private": true,
  "dependencies": {
    "@react-native-picker/picker": "2.7.5",
    "expo": "~51.0.22",
    "expo-image": "~1.12.14",
    "moment": "^2.30.1",
    "react": "~18.2.0",
    "react-native": "0.74.3"
  }
}
